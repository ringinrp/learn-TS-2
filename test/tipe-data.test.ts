describe('Data type', () => {
    it("should must declare", function(){
        const name: string = "Ringin Restu Pati";
        const balance: number = 1000;
        const isVip: boolean = true;

        console.info(name)
        console.info(balance)
        console.info(isVip)
    });
});;