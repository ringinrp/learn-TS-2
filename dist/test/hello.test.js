"use strict";
describe('Hello', function () {
    it("should say hello", function () {
        const name = "Ringin Restu Pati";
        expect(name).toBe("Ringin Restu Pati"); //untuk menjalankannya npx jest test/hello.test.ts
    });
});
